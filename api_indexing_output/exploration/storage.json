{
  "total_events": 303,
  "noise_filtered": 20178,
  "tokens": [
    "sessionStorage['session_id'] \u2014 session ID candidate (~21 chars); single write observed during session (storage event #53)",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forUserContext.userState.token'] \u2014 primary auth token string held in Next.js bootstrap data; appears and later cleared to null/None",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forUserContext.userState.cvIdToken'] \u2014 ID token string in userState; appears in initial Next.js data then cleared",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forUserContext.userState.cvAccessToken'] \u2014 access token string in userState; appears in initial Next.js data then cleared",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forCommonStaticContext.serverCookies.mdt-session-id'] \u2014 session cookie value surfaced into Next.js data; string token that mirrors a server-set session cookie",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forStaticContext.hadUtmLocationTokenOnServer'] \u2014 boolean flag indicating a UTM-location-related token was present on the server",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forCommonStaticContext.configuration.enableAuthLogging'] \u2014 boolean auth logging feature flag controlling how auth events/tokens are logged",
    "window['__NEXT_DATA__.props.pageProps.forProviders.forStaticContext.configuration.enableAuthLogging'] \u2014 boolean auth logging flag at a static context level",
    "window['default_gsi.Kc.g.enable_browsing_session_id'] \u2014 Google Identity Services config flag controlling use of browsing_session_id token",
    "window['default_gsi.Kc.g.enable_gis_oauth_button_flow_popup'] \u2014 Google Identity Services OAuth flow config (popup mode)",
    "window['default_gsi.Kc.g.enable_gis_oauth_button_flow_redirect'] \u2014 Google Identity Services OAuth flow config (redirect mode)",
    "window['default_gsi.Kc.g.enable_itp_support_in_oauth'] \u2014 Google Identity Services config related to ITP / cookie handling for OAuth flows",
    "window['google.accounts.oauth2.GoogleIdentityServicesErrorType.*'] \u2014 string constants for OAuth error types, indicating presence of Google OAuth client library (error codes, not tokens themselves)"
  ],
  "data_blocks": [
    "localStorage['CVSavedSuggestions'] \u2014 JSON array (~2 chars) likely holding recent search suggestions or viewed vehicles; currently empty in this capture",
    "sessionStorage['CvnaInitialViewState'] \u2014 JSON value (~21 chars) representing initial view/state for the Carvana app (e.g., route, filters, user context snapshot)",
    "localStorage['vdp/alertSignUpVehicles/v1'] \u2014 JSON array (~2 chars) storing vehicle alert sign-up data (e.g., watched VINs); empty at time of capture"
  ],
  "narrative": "Overall there are 303 events: 282 storage (cookies/localStorage/sessionStorage) and 21 window property events. Storage is all from a single origin (https://www.carvana.com). Most keys are analytics, experimentation, or UI/feature flags (_nxt_*, snowplow*, dd_*, spdt-*, car-merch-*, quali-*, topics*, etc.), which are treated as noise and counted but not cataloged individually.\n\nTokens:\n- A sessionStorage key `session_id` appears once with a short (~21 char) opaque value. By name and usage this behaves like a client-side session identifier, likely used to correlate the browser session with server state or analytics.\n- In window properties, Next.js bootstrap data `__NEXT_DATA__` includes a `forProviders.forUserContext.userState` object with several token-related fields:\n  - `userState.token` (string) \u2014 primary auth token for the logged-in context. It is present early and later transitions to None/null, suggesting a lifecycle where the token is removed from the bootstrap object after hydration or when user context is cleared.\n  - `userState.cvIdToken` (string) \u2014 an ID token representing the user identity (likely JWT-shaped in the real page, though the raw value is not inspected here). It also appears then is nulled out.\n  - `userState.cvAccessToken` (string) \u2014 an access token used for API calls. Same pattern: present initially, then cleared, implying that the runtime app moves this into a more private store (e.g., in-memory auth module) and scrubs it from the global data structure.\n  - `userState.isValidToken` (boolean) \u2014 indicates whether the current token is considered valid; toggling here reflects auth status.\n- `__NEXT_DATA__.props.pageProps.forProviders.forCommonStaticContext.serverCookies.mdt-session-id` exposes a server-side session cookie value into the JS bootstrap. This is a string token mirroring the `mdt-session-id` cookie; it links browser cookies to app-level session handling.\n- Several config/feature flags around auth:\n  - `forStaticContext.configuration.enableAuthLogging` and `forCommonStaticContext.configuration.enableAuthLogging` (booleans) control whether auth events/tokens are logged for debugging or telemetry.\n  - `forStaticContext.hadUtmLocationTokenOnServer` (boolean) indicates the server observed some UTM-location-related token; this is metadata about marketing attribution tokens rather than an auth token itself.\n- Google Identity Services (GIS) / OAuth configuration is present on `window.default_gsi` and `window.google.accounts.oauth2`:\n  - `default_gsi.Kc.g.enable_browsing_session_id`, `enable_gis_oauth_button_flow_popup`, `enable_gis_oauth_button_flow_redirect`, and `enable_itp_support_in_oauth` are booleans configuring how GIS manages session IDs and OAuth flows (popup vs redirect, ITP support). These are not tokens themselves, but they govern issuance and handling of OAuth tokens.\n  - `google.accounts.oauth2.GoogleIdentityServicesErrorType.*` are string constants for various OAuth error types, confirming the presence of the GIS OAuth library but not exposing actual tokens.\n- Various webpack module cache paths reference `InvalidTokenError`, `turnstileTokenSchema`, `themeTokens`, and auth event names (login/logout/refresh/tokenRefreshed). These show that the bundle includes structured logic around token validation and events, but the actual token secrets are not stored there.\n\nData blocks:\n- `localStorage['CVSavedSuggestions']` and `localStorage['vdp/alertSignUpVehicles/v1']` are JSON arrays that are currently empty (`[]`). By key naming, they are used to cache user-centric data:\n  - `CVSavedSuggestions` likely holds recent search suggestions or previously entered queries/locations.\n  - `vdp/alertSignUpVehicles/v1` likely holds a list of vehicles or alert subscriptions for vehicle detail pages (VDP). Even though they are empty in this capture, they are structured storage for user data.\n- `sessionStorage['CvnaInitialViewState']` is a small JSON value representing the initial view state for the SPA \u2014 probably including route, filters, and some derived context. It is used as a bootstrap snapshot for the client-side app.\n\nNoise:\n- A very large majority of storage and window-property changes (noise_filtered \u2248 20k individual change events) are analytics, experimentation, and utility keys: snowplow queues, Datadog RUM session/tab IDs (`dd_rum_session_id`, `dd_tab_id`), Microsoft/Bing UET IDs (`_uetsid`, `_uetvid`), Google conversion IDs (`_gcl_ls`), experimentation and merchandising flags (`car-merch-*`, `quali-*`, `merchMaintainScrollPosition`), Snowplow outQueues, `spdt-*` tracking keys, Modernizr feature flags, AudioEye accessibility settings, and general topics/interest tracking. These are counted as noise and not detailed individually.\n\nOrigin of the requested value:\n- A cross-store search for the literal value \"TRACE VALUE\" across network responses, storage (cookies/localStorage/sessionStorage), and window properties returned zero matches. That specific value does not appear anywhere in this capture, so its origin cannot be traced. All identified tokens and data blocks above are based on key names, structure, and context rather than a match to the requested value.\n\nAuth/storage story:\n- Auth for this Carvana page appears to use a combination of server cookies (`mdt-session-id`), a client-visible session identifier in sessionStorage (`session_id`), and access/ID tokens exposed briefly in the Next.js bootstrap data (`__NEXT_DATA__.props.pageProps.forProviders.forUserContext.userState.*`). The tokens are present during initial render and then cleared from the global object, suggesting that the live app moves them into a more controlled in-memory auth module and avoids long-lived exposure on `window` or in persistent storage. Google Identity Services is available for OAuth flows, but actual OAuth access/ID tokens from Google are not visible in this capture. No CSRF-specific cookies or headers are obviously stored in browser storage. Overall, sensitive auth material is mostly transient in window bootstrap data and not persisted in long-lived localStorage keys."
}